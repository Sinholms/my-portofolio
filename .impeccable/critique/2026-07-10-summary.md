# Polish Pass Summary

## Trend

```
28/40 ──► 33/40 ──► 37/40
(base)    (polish)  (critique)
```

P0: 0 throughout. P1: 2→2→1. P2: (first) 0→2→1. All issues reduced.

## What Was Done

| Section | Fix | Impact |
|---------|-----|--------|
| `servicessection.tsx` | Rewrote generic 3-card grid → outcome/proof strip with real project names (LPSE-X, Nusa-Q) | P1 — eliminates worst AI-slop tell |
| `herosection.tsx` | Demoted bare "Positioning" eyebrow to plain label; rewrote subtitle around value, not stack list | P1 — recruiter sees outcome first |
| `projectsection.tsx` | `project.type` text → white-backed bordered badge (full contrast) | P1 — badge system now consistent |
| `experiencesection.tsx` | Named "vaskoyudha" team; converted highlights to concrete outcomes (SVG risk map, shared GitHub workflow) | P2 — trust valley fixed |
| `aboutsection.tsx` | `text-neutral-300` → `text-white` (dark section); rewrote around working style/collaboration | P3 — readability + differentiation |
| `footer.tsx` | `neutral-400/300` → `text-white` tagline; changed to "Open to junior, internship & freelance work" | P3 — signals availability |
| `header.tsx` | Added persistent mobile "Hire me" CTA (`nb-btn-primary`, `md:hidden`) | Mobile — conversion path complete |
| `contactsection.tsx` | Client-side email regex validation + inline nb-accent-3 error block | P2 — reduces bad submissions |
| Shadow tokens | 4 hardcoded offset shadows → `shadow-nb-sm` / `shadow-nb-lg` in about + experience sections | P2 — design system discipline |

## Remaining Issues (from 37/40)

**[P1] 3 kickers lose their ink border.** `aboutsection.tsx:50`, `experiencesection.tsx:113`, `footer.tsx:14` use `border-3 border-nb-accent bg-nb-accent` → border == bg, invisible 3px border. Fix: `border-nb-border`.

**[P2] Three consecutive 3-up grids.** About principles (3 cards) → Services (3 cards) → Skills (3 columns). Repetitive rhythm. Fix: vary one (About → horizontal strip, or Skills → 2-col).

**[P3] Services subtitle reads generic.** "across the modern web stack" contradicts proof-led cards below.

**[P3] Services CTAs all jump to `#projects`.** No deep-linking to specific project cards.

## DO

- Keep ink borders visible — kicker badges, cards, buttons must show `border-3` contrast
- Lead with outcome in copy (what was built, not what tech was used)
- Name real projects, teams, and clients — generic labels erode trust
- Keep one accent per block — neobrutalism discipline
- Mobile CTA must always be one tap away
- Validate contact form client-side + server-side
- Write frontmatter with `total_score` + `p0_count` when persisting critique snapshots

## DON'T

- Don't use `as any`, `@ts-ignore`, or type suppressions — TypeScript `strict: true`
- Don't use text that matches border color (invisible border = broken badge spec)
- Don't stack three identical 3-column grid layouts consecutively — vary the rhythm
- Don't use generic section titles ("What I Do", "My Services") — lead with proof
- Don't let the "Positioning"/"Tracked" eyebrow pattern appear bare — always style as `nb-section-eyebrow`
- Don't add zero-blur shadow as hardcoded value — use `shadow-nb-*` tokens
- Don't delegate visual work to `visual-engineering` category — it hangs in this environment; use `general` or direct edits
- Don't claim "done" without `npm run build` verification
