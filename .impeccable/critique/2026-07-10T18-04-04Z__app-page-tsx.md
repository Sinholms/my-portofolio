---
timestamp: 2026-07-10T18-04-04Z
slug: app-page-tsx
total_score: 37
p0_count: 0
p1_count: 1
p2_count: 1
p3_count: 2
---
# Critique — Muhammad Falih Akbar Portfolio (target: `app/page.tsx` + 7 sections)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | No scroll-progress cue; mobile menu state, form states good. |
| 2 | Match System / Real World | 4 | Recruiter-facing copy plain and concrete. |
| 3 | User Control and Freedom | 4 | Persistent nav, back-to-top, closeable mobile menu. |
| 4 | Consistency and Standards | 3 | 3 kickers use border==bg (loses ink border) — breaks badge spec. |
| 5 | Error Prevention | 4 | Email regex + honeypot + inline validation. |
| 6 | Recognition Rather Than Recall | 4 | Single page, persistent header, all paths visible. |
| 7 | Flexibility and Efficiency | 3 | Mobile "Hire me" CTA added; no keyboard deep-links beyond nav. |
| 8 | Aesthetic and Minimalist | 4 | Disciplined neobrutalism; loud but structured. |
| 9 | Error Recovery | 4 | Bordered Hot-Rose error block, plain-language messages. |
| 10 | Help and Documentation | 4 | Self-evident portfolio; no docs needed. |
| **Total** | | **37/40** | **Excellent** — minor polish only. |

## Anti-Patterns Verdict

**LLM assessment (Assessment A):** NOT AI-slop. The page now reads as a crafted, opinionated portfolio. The prior generic-3-card Services tell is gone: it is now proof-led (LPSE-X risk map, Nusa-Q GitHub workflow, self-citation). Bare tracked eyebrows are eliminated — the hero "Positioning" label is correctly demoted; all 7 section kickers use the bordered badge. Project `type` is a white-backed bordered badge, not low-contrast text. The deliberate kicker-badge system is the strongest anti-slop signal on the page: a real brand device, consistently applied. The one remaining consistency catch: 3 kickers (about, experience, footer) use border color == bg color, so the ink border is invisible.

**Deterministic scan (Assessment B):** `detect.mjs` over `app/page.tsx` and full `app`+`components` tree returned EXIT 0 (clean) — 0 findings. Same clean result as the pre-fix run. No false positives.

**Visual overlays:** NOT produced — no browser automation available; static CLI pass only.

## Overall Impression
A genuinely polished, recruiter-aware neobrutalist portfolio. The prior critique's P1 items (Services rewrite, bare eyebrows) are fully resolved. The page moved from "good, on-trend" in the first critique (28/40) through a polish pass (33/40) to "excellent, minor polish only" (37/40). The remaining issues are edge-case design-system consistency and layout rhythm, not strategic gaps.

## What's Working
1. **Proof-led Services** — names LPSE-X, Nusa-Q, and self-cites the portfolio; converts the weakest section into a trust builder.
2. **Executed neobrutalist craft** — consistent hard shadows, square corners, single family, one-accent-per-block; reads as intentional.
3. **Reachable conversion path** — desktop + mobile "Hire me" CTAs, persistent header, footer close, working form with inline validation.

## Priority Issues (remaining)
**[P1] 3 kicker badges lose their ink border.** aboutsection.tsx:50, experiencesection.tsx:113, footer.tsx:14 use `border-3 border-nb-accent bg-nb-accent` (border == bg), so the 3px ink border is invisible — violates DESIGN.md's "bordered kicker badge" rule. *Fix:* change `border-nb-accent` → `border-nb-border` in those three.

**[P2] Three consecutive 3-up card grids create rhythm repetition.** Page order: About principles (3 cards), Services (3 cards), Skills (3 columns) — stack three near-identical 3-column rhythms. *Fix:* vary one (e.g., About as horizontal strip on dark, or Skills as 2-col).

**[P3] Services subtitle reads generic.** servicessection.tsx:61-63: "across the modern web stack" tail contradicts the proof-led cards above. *Fix:* reword to reference real projects.

**[P3] Services CTAs all jump to `#projects` top, not specific card.** href:'#projects' even for "View LPSE-X"/"See Nusa-Q". *Fix:* add project ids and deep-link, or accept as-is given short section.

## Persona Red Flags
- **Jordan (First-Timer):** Role + value in hero <2s. ✓
- **Riley (Stress Tester):** Contact form has email regex, honeypot, disabled-while-pending, error block. ✓
- **Casey (Distracted Mobile):** Mobile "Hire me" CTA fail **resolved** — persistent nb-btn-primary next to hamburger + full-width CTA inside menu. Back-to-top always reachable. ✓
- **Rina (Recruiter/HRD scanner):** Gets stack (Skills), output (Projects), snapshot ("What HRD can expect") in first screen. Services now names real projects. Contact one tap. Minor: Junior Developer entry reads "Freelance & Team Projects · vaskoyudha" — truthful but employment framing is soft.

## Minor Observations
- Skill level dots (`skillsection.tsx:133`) use `rounded-full` — only rounded element on page; breaks the universal square-corner rule.
- Footer copyright "© 2026" — verify year is intentional.
- Project `type` badge whitespace `bg-white/90` over accent fill is legible; fix holds.
- Hero decorative shapes `lg:block` only — mobile hero clean.

## Questions to Consider
1. Three 3-up grids in a row — does the page start to feel templated mid-scroll?
2. "Team-ready delivery" rests on "shared GitHub workflow / code reviews" — concrete enough, or needs an artifact?
3. About's three principles overlap with Services "What I build" — is About the middle column redundant?
4. Every proof block is qualitative — is the portfolio one outcome number away from being decisively convincing?
